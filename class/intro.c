#include<stdio.h>
int main()
{
    int a,b;
    scanf("  %d  %d",&a,&b);
   int sum=a+b;
   int mul=a*b;
   float div=a/b;

    printf(" the sum of no is %d   ",sum);
      //  printf(" the sum of no is %d   ",);

    printf(" the mul of no is %d   ",mul);
        printf(" the div of no is %f   ",div);

return 0;

}